import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Services — Core Capabilities & Industry Coverage",
  description:
    "Six core security capabilities across 12 industries in Maharashtra — VIP protection, manned guarding, events, surveillance, rapid response, and corporate programs.",
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
