import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Why ARMOURIXX — Our Operational Advantage",
  description:
    "Discover what sets ARMOURIXX apart — elite personnel, technology-driven ops, full accountability, and rapid response across Maharashtra.",
};

export default function WhyUsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
